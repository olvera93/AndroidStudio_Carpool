<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carpool">

    <!-- Pedir permiso para la localización -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Carpool">
        <activity android:name=".controllers.Register"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity android:name=".controllers.VerPerfil"
            android:label="@string/view_profile_user">
    </activity>

        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme"
            android:label="@string/login"
            android:parentActivityName=".controllers.Register">
           </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".controllers.TravelScreen"
            android:label="@string/travel_screen"/>

        <activity android:name=".principalscreen"
            android:label="@string/request_trip"
            android:theme="@style/AppTheme.NoActionBar"/>


        <activity android:name=".RecyclerAdapter.TravelHistory"
            android:label="@string/travel_histories"></activity>





    </application>

</manifest>